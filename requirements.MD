housing management app:
it involves calculation of the rent paid,
data inserted by service desk.
* subsidy coming from main plus province

    ->updates by job
* payments of rent:
    - people pay check or pay cash which service desk marks them as pending/or by automated 
system when they from mobile app/something we dont know, 
    - then by batch job these data are updated that payment received. data coming from multiple source, multiple form of data
    - this rent will be created by system, at the end of month, this is another job.
    - rent due, rent overdue, these emails will be generated by batch job.
* maintenance data->
    - service desk inserts data, needs repair
    - admin person sees it, they create work order 
    - work is done, these data coming via csv, text files, many files or sometime
    just inputted by service desk. ok so these data are updated by jobs.
* Reporting->
    - sometime admin person wants a report of this month revenue, total rent collected, total maintenance spend done.
    then we run on demand job. it generates report.
    - sometime admin/accounting person wants detail report about maintenance expense, these are exported with csv.
    these are also on demand job run.
* custom query->
    - sometime miscellenous report audit comes, like which people received those benefits,
    or taken these kind of homes, that time i write manual queries, run it on test,
    if data pattern looks good, they tell to run in prod.
* Most update queries are scheduled overnight.
* Profile, income, eligibility data -> updated by job overnight. The data is coming from multiple sites.
reading multiple format of data, also call API of other service to get some data. but mostly
coming via text files.
* There should be UI to add user, add permissions, add Roles.

### Type of persons in the system:
- service desk people/client service people. they also have office location,
set to their ID. they can input various data as requested by the client.
Like somebody wants to apply for a rental, they give basic info like what work
they do, they email service desk statements, they attach it. So like applying.
Service desk can also update a number of things on the profile of the clients. its like 
on bhealf. Most of the data inthe system should have some column that who updated(like 
which agent). Thse are all powered by REST API. 
- HR people, they can pull variety kind of basic reports, like payments, vacancy etc.most of them
are like per person reprots, or for all basic kind of reports.
- Developer/program admin person can see almost everything, they can generate reports, they can
create people, change.

### Additional for spring job:
- we need cleanup process, which cleans job after set time like 3-5 days.
- spring's default tables for batch job are not optimized at all. We need to put index on job execution,
steps these kind of columns.


Features to work
- option to create a building,and address then suite(with rent)
- option to rent it to somebody, terminate tenancy with adding note to tenancy;
- option to create rent with spring scheduler, spring batch. also option to
manualy trigger job, passing data. 

these data could be, all the maintenances that are done/more detail
all the rents are collected recently,
more example will come. 
- option to mark a rent as paid
- api to search all suites of a city that are not occupied,
- api to show building details, with suite list, suite occupancy status, rent.
- will do tenant application, approval these things

### Done:
- address api is functional, crud api.
- building api is functional, can create building with address, and update building.
- suite creation api is functional, detail is functional
- option to rent it to somebody is done, creating tenancy.
- terminate tenancy with note(update tenancy)

## what I am doing for converting monolith to microservice:
- I am converting my tenancy, rent,tennat profile these things to another microservice.
- i identified the domain, context area. I kind of grouped the things that i want to move, in a package.[kori nai but bolbo]
- i copied the project, both project now using same db, same models. 
- mciroservicre er vitore, ami ekhon je model gula ansi, r je service gula ansi, ogular vetore onno repository er jesob reference silo
segula clean kore, just id rakhsi, ejonno repository kisu bad disi, as oi model gula ami r retrtieve korte parbona.
just id rakhsi. plus database e migration kisu lagte pare. Also entity model update kora lagse, ekhon say Rent Entity er vitore suite nai r, sudu suite id. r entity model er je Suite likhte partam,
seta r nai, Many to Many, One to many likhte partam segula ar nai. associattion gula r korte parbona, ejonno aro onno code eo change kora lagbe.
ager 
```
@JoinColumn(name = "suite_id")
    @ManyToOne
    @JsonIgnore
    private Suite suite;
```
now,
```
@Column(name = "suite_id")
    private Long suiteId;

```
- I am starting to think that, i should call the other microservice syncrhonously to validate these things instead of assuming that the suiteid i am getting from the frotnend to create a tenancy, is always correct.
- kisu new api add kora lagbe microservice e now ar ogular fein client banan lagbe parent e, as kisu detail lagle ogula call diye ene nibo..
- ekta challange pailam je, api er response e j model declare korsilam, ogula abar parent microservice e anai lagse. like rentprice model for rent price list(of a suite) API. tahole amar model change korle microsevice e , eta parent eo change kora lagbe.
- erpore dekhbo egula functional hoy naki microservice e, still same db.
- then original project e jaye jekhane jekhane direct call hossilo, but ekhon mciroservice e chole gese(like tenancy, rent)
egula jaygay jaya repostiory delete kroe dei, ar egula k rest api diya replace kori. feign client use kore kori
so dekhte code ta mone hobe ami ekta serevice call e dissi.
- ekta problem hoise je, amra jokhon sob kisu ekta monlolith e silo, kisu jinis null ok silo, ogula k microservice e api te convert kore, parent theke okhane call kore data anle, null e ki error throw korbo, naki null ok egula define kora lagse.
such as: getLatestRentPrice (from rent microservice) for a suite, jodi rent ektao na thake,
taile age null pathaitam frotnend e null jaito rent, kintu ekhon etake rent microservice e api banaisi,
r throw korsi 422, parent microservice e just try cath diya suite er rent json response e null pathai disi.
hoyto onno kono case e trycatch niya null diya response pathano uchit hobena, if mciroserive theke error ase, taile sekhtere hoyto error e pathan lagbe.
ejonno feing client er error decoder banaisi, sekhan theke error rethrow kortesi, jate almost monolith e jemne method theke error throw korto kono kisu na paile, ekhaneo jate kore ovabe.
- erpore gradually delete kore dei, mciroservice theke extra controller, model gula.

evabe naki korake **Strangler Fig** pattern bole. Strangler Fig" pattern where you incrementally extract functionality from the monolith into separate services while still relying on the shared database for data storage.

- r main project theke delete kroe dei microservice er model, service gula, eta kortesi, etar jonno code update kora lagbe, lots of reference
- then integration test update kori main project e, api call diya, r microservice er theke integration test
jegula main service er silo ogula fele dei.
-also dekhlam DTO to age jantam ki, ekhon to feign client diya hit krote dto janina, dto copy kora lagse.
so ekhon dto gula consistent rakha lagtese. maybe better way hote pare package kora sepearte.
- erkm vabe first e syncrhonously kaj korle duta microservice, ami pore etake event based korbo event driven hobe. 
## Database migration: most tricky:
- ami jeta korsi first e main db er schema only backup nisi sql
`pg_dump --schema-only --no-owner --no-privileges -h hostname -p 27264 -U username -d tenant_jobs_db > schema_backup.sql`
- oi backup theke sudhu jegula microservice er lagbe osob rakhsi baki sob commented out
- also relationship er gula edit korsi, jesob relationship ekhon r possible na, segula comment out/change korsi, for example, tenant history er suiteId suite table k point korto ekhon r korbena.
- ekhon new db create kore, ekta migration file e flyaway te eta diya dibo. actually eto table eto constraint, ekta flyawy file e na, ami gradually kortesi ekta table kore ekta migration, plus orm error dissilo baki table koi, ejonno ekta ekta kore migraion for one table, r baki orm e entity,tablename egula comment out korsi temporarily
- so flyaway first migration hisabe esob table niye nibe
- erpore plan ase ager db theke sudu osob table er data backup nibo, then ogula pgdump ba konovabe insert korbo new db te, constraint disable kora lage ekhane kokhono kohono temporarily, amar lage nai
`psql -h <new_host> -p <new_port> -U <new_username> -d <new_database> < data_backup.sql`
- pgdump theke j sql nisi ogulate somossa hoise like primary key serial hossilona. karon ami migration kore table bananor somoy just declare kore disi bigserial in the beginning,
but postgres theke jokhon dump niye migration chalaisi, dekhi postgres eta onnovabe korse. id primary key autoincrement korte
postgres korse ki, id type bigint rakhse ar serial pore banay assign korse. eta change kroe bigserial kora jabena.
however ami jokhon migration first e likhsi id type e silo bigserial.
so postgres dump generated sql file use korar karone, implementation ta vinno hoye gese.
ejonno jpa repostiry automatic incremental id generate korte partesilona, somadhan silo entity model e
diye deya sequence no. psotgres er alada kore serial implement koray entity model r dhorte partesilona automatic.
Now, ami to id rakhsi basemodel e, ejonno amr base model er id override kora lagse onno model e
r sequence name bole deya lagse. egula hotona jodi ami postgres implementaion ondher moto copy na kortam.
```java
@Entity
@AttributeOverrides({
        @AttributeOverride(name = "id", column = @Column(name = "id", unique = true, nullable = false))
})
@Table(name = "rent_price")
public class RentPrice extends BaseEntity {

    //although id exists in base entity, i had to override like this to manually connect
     //to the sequence psotgres dump generated 
    @GeneratedValue(strategy = GenerationType.SEQUENCE, generator = "rent_price_id_seq")
    @SequenceGenerator(name = "rent_price_id_seq", sequenceName = "rent_price_id_seq", allocationSize = 1)
    @JsonProperty(access = JsonProperty.Access.READ_ONLY)
    private Long id;

```
- actually egula koreo kaj hoynai, nana somoossa then migration file falay diya, ager migration dekhe dekhe thik jevabe
table create korsilam ovabe table create kori abar, migration file manually likhi. ekhane kosto kora lagse je
postgres dump r ager migration dekhe compare kore kivabe likhle ager moto hobe r predictable hobe r
sequence niye issue hobena like(not declaring sequence but having bigserial) egula kore mgiration abar kora lagse.
like amar nijer migration banan lagse, say, ami first migration e ekta table banaisioam ekrokom, porer sob migration study kroe, new migraiton banaisi jate table schema match hoy, serial  index e somossa na hoy.